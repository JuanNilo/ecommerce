
import { Colors } from "@/app/styles/style"

const { tertiary } = Colors

export default function Slider(){
    return(
        <div className="h-[90%] w-[80%] flex mx-auto rounded-lg overflow-hidden" style={{backgroundColor: tertiary}}>
        </div>
    )
}